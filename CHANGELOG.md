# 変更履歴

このファイルは KoeNote のリリースごとの変更を記録します。
最新版のダウンロードは [releases/latest](https://github.com/satoshi-hashimoto52/KoeNote/releases/latest) から行えます。

---

## v0.1.1

低音量のマイク入力が無音として扱われ、文字起こしされない問題を修正した
メンテナンスリリースです（[Issue #0019](docs/issues/0019-input-gain-calibration-and-low-volume-detection.md)）。

### 修正

- **本体マイクなど低音量入力の文字起こし欠落を修正しました。**
  固定しきい値 `MIN_RMS = 0.006` が、Whisper へ渡す前に低音量の窓を破棄していました。
  本体マイクで遠方の会話を録ると全編が -48 dBFS 前後になり、実測ノイズフロア
  -61.8 dBFS の 7.4 倍にあたるこのしきい値では **463 窓中 366 窓（79.0%）** が
  捨てられ、61 分の会議が 4,270 字しか残りませんでした。
  さらに、捨てたことが診断ログにも残らない状態でした。
- **temperature fallback を有効化しました。**
  スカラー指定により faster-whisper の再デコードが働かず、
  「はい」の反復ハルシネーションが確定テキストへ入っていました（実測 446 字）。
- **反復ハルシネーション対策を追加しました。**
  fallback を通しても反復のままのセグメントは確定テキストへ保存せず、
  診断へ記録します。
- **設定 JSON の同時保存で失敗する不具合を修正しました。**
  一時ファイル名が固定かつ非直列だったため、保存が重なると `ENOENT` になりました
  （旧実装は 30 並列で 29 件失敗）。

### 追加

- **入力モード**（自動 / マイク / 内部音声 / カスタム）。
  「自動」は BlackHole などの仮想オーディオデバイスを判定して振り分けます。
  判定結果は設定画面に表示され、手動で上書きできます。
- **デバイス別プリセット**。設定はデバイスごとに保存され、
  入力デバイスが変わったときは、そのデバイス用のプリセットを解決し直します。
- **マイク入力の自動音量補正**。発話レベルに追従する緩慢なゲインで、
  クリッピングを防ぎながら文字起こしに適したレベルへ揃えます。
  無音・環境音だけの区間ではゲインを更新しないため、ノイズを持ち上げません。
- **入力レベルメーター**。録音画面に現在の入力レベルと適用中の入力モードを表示します。
  色だけでなく記号と短い語でも状態が分かります。
- **入力テスト（キャリブレーション）**。環境音 5 秒と遠方発話 18 秒を測定し、
  ノイズフロア・発話レベル・SNR・推定通過率・推奨ゲイン・総合判定を表示します。
  推奨設定をその場で適用できます。
- **低音量・低 SNR・音割れの警告**。原因を区別して通知します。
  発話が無いだけの静かな時間は警告しません。
- **`transcript_segments.json` の生成**。確定した発話を
  セッション基準の絶対経過秒つきで保存します（旧 Issue #0004 を統合）。
- **音量状態の診断ログ**。ノイズフロア・生 RMS・補正後 RMS・適用ゲイン・
  ピーク・クリッピング防止の介入・スキップ秒数を 60 秒ごとに集計します。

### 変更されないもの

- **`recording.wav` には補正前の原音を保存します。**
  音量補正は Whisper へ渡す解析用の音声にだけ適用されます。
  事後の再解析のために原本を壊しません。
- 録音時間・サンプル数・タイムスタンプは補正の前後で変わりません。

### 実機検証（2026-09-14）

55.5 分の実会議で確認しました。

| 指標 | 修正前（v0.1.0） | 修正後（v0.1.1） |
|---|---|---|
| 文字数 | 4,270 字 / 61.8 分 | **14,996 字 / 55.5 分** |
| 1 分あたり | 69.1 字 | **270.3 字** |
| レベル起因のスキップ | 79.0% | **0.0%** |
| 反復ハルシネーション | 1 件（446 字） | **0 件** |

### 未確認事項

**会議室の最遠席・約 -45〜-48 dBFS の条件での実測は、まだ行えていません。**

上記の実機検証は入力レベルが高い条件（1 分窓 RMS 中央値 -33.2 dBFS）で
行われており、本リリースが本来対象とする遠方・低音量条件での効果は
「マイクに近い良い録音条件だった」効果と分離できていません。

非常に低い入力条件で使う場合は、**事前に入力テストを実行**して
総合判定と推奨ゲインを確認してください。
再現条件と確認項目は
[Issue #0019 の「将来の確認事項」](docs/issues/0019-input-gain-calibration-and-low-volume-detection.md)
に記録しています。

---

## v0.1.0

2026-08-31 公開。初回リリース。

[リリースページ](https://github.com/satoshi-hashimoto52/KoeNote/releases/tag/v0.1.0)
